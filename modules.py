# Importing the fibo module and renaming it as f.
import fibo as f

# Calling the function `fib` from the module `fibo` and passing the value `100` to it.
f.fib(100)

# Calling the function `fib2` from the module `fibo` and passing the value `100` to it.
f.fib2(100)